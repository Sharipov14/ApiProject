export class Distribution {
    constructor(id, workerId, projectId, dateStart, hours) {
        this.id = id;
        this.workerId = workerId;
        this.projectId = projectId;
        this.dateStart = dateStart;
        this.hours = hours;
    }
}
//# sourceMappingURL=distributions.js.map